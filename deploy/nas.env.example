# Copy to .env next to compose.yaml. Use a long random password (letters and
# digits only, since it is embedded in a connection URL).
POSTGRES_PASSWORD=change-me
SITE_URL=http://192.168.100.216:8000
APP_PORT=8000
SUPPORT_EMAIL=
OPENAI_API_KEY=
