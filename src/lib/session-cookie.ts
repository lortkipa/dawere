/**
 * Cookie names, kept free of imports so proxy.ts can read them without
 * pulling the database client into the proxy bundle.
 */
export const SESSION_COOKIE = 'dawere_session';
export const VISITOR_COOKIE = 'dawere_visitor';
